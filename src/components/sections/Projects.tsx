import { ArrowUpRight } from 'lucide-react';
import { projects } from '@/data/projects';
import { Reveal } from '@/components/ui/Reveal';
import { SectionHeading } from '@/components/ui/SectionHeading';

export function Projects() { return <section id="realizacje" className="section-pad overflow-hidden border-b border-white/10 bg-apex-ink"><div className="mx-auto max-w-[1440px] px-5 sm:px-8 lg:px-12"><Reveal><SectionHeading index="03" eyebrow="Selected work" title={<>Samochody,<br /><span className="text-white/25">które jadą dalej.</span></>} intro="Przykładowe obszary pracy. Marka samochodu nie oznacza autoryzacji producenta — obsługujemy auta różnych marek." /></Reveal><div className="projects-scroller mt-16 flex snap-x gap-4 overflow-x-auto pb-5">{projects.map((project, i) => <Reveal key={project.brand} delay={i * .05} className="min-w-[78vw] snap-start sm:min-w-[390px] lg:min-w-[calc(25%-12px)]"><article className={`project-card ${project.tone} relative aspect-[.86] overflow-hidden border border-white/10 p-6`}><div className="absolute inset-0 bg-[linear-gradient(145deg,rgba(255,255,255,.12),transparent_38%,rgba(0,0,0,.7))]" /><div className="relative flex h-full flex-col justify-between"><div className="flex items-center justify-between text-[9px] font-bold uppercase tracking-[0.18em] text-white/55"><span>0{i + 1} / case file</span><ArrowUpRight size={16} /></div><div><p className="text-[9px] font-bold uppercase tracking-[0.22em] text-white/55">{project.label}</p><h3 className="mt-2 font-display text-5xl font-black uppercase leading-none tracking-[-0.06em] text-white">{project.brand}</h3><p className="mt-3 text-xs text-white/55">{project.detail}</p></div></div></article></Reveal>)}</div><p className="mt-3 text-[9px] uppercase tracking-[0.16em] text-white/25">Wizualizacje koncepcyjne / do zastąpienia realnymi zdjęciami realizacji.</p></div></section>; }
