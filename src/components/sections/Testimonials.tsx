import { Quote, Star } from 'lucide-react';
import { testimonials } from '@/data/testimonials';
import { Reveal } from '@/components/ui/Reveal';
import { SectionHeading } from '@/components/ui/SectionHeading';

export function Testimonials() { return <section id="opinie" className="section-pad border-b border-white/10 bg-apex-ink"><div className="mx-auto max-w-[1440px] px-5 sm:px-8 lg:px-12"><Reveal><SectionHeading index="05" eyebrow="Testimonials" title={<>Słowa, które<br /><span className="text-white/25">mają wagę.</span></>} /></Reveal><div className="mt-16 grid gap-4 lg:grid-cols-3">{testimonials.map((item, i) => <Reveal key={item.quote} delay={i * .07}><figure className="flex min-h-[260px] flex-col justify-between border border-white/10 bg-apex-panel p-6 sm:p-8"><div className="flex items-center justify-between"><Quote size={24} className="text-apex-red" /><div className="flex gap-1 text-apex-red">{Array.from({ length: 5 }).map((_, index) => <Star key={index} size={12} fill="currentColor" />)}</div></div><blockquote className="mt-10 font-display text-2xl font-bold uppercase leading-tight tracking-[-0.03em] text-white">“{item.quote}”</blockquote><figcaption className="mt-7 border-t border-white/10 pt-4 text-[9px] font-bold uppercase tracking-[0.16em] text-white/35">{item.author} · {item.meta}</figcaption></figure></Reveal>)}</div><p className="mt-5 text-[9px] uppercase tracking-[0.16em] text-apex-orange/65">Placeholdery opinii — przed publikacją zastąp zweryfikowanymi opiniami klientów.</p></div></section>; }
